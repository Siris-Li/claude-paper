{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup",
        "hooks": [
          {
            "type": "command",
            "command": "& \"D:/Programs/Git/bin/bash.exe\" -lc 'PLUGIN_ROOT=\"$CLAUDE_PLUGIN_ROOT\"; if command -v cygpath >/dev/null 2>&1; then PLUGIN_ROOT=\"$(cygpath -u \"$PLUGIN_ROOT\" 2>/dev/null || printf \"%s\" \"$PLUGIN_ROOT\")\"; fi; bash \"$PLUGIN_ROOT/hooks/check-install.sh\"'",
            "statusMessage": "Initializing Claude Paper..."
          }
        ]
      }
    ]
  }
}
